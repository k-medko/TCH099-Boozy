<resources>

    <!-- Style pour les boutons de catégorie -->
    <style name="CategoryButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:background">@drawable/category_button_background</item>
        <item name="backgroundTint">@null</item> <!-- IMPORTANT pour désactiver la teinte violette -->
        <item name="strokeColor">#CCCCCC</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Custom style pour désactiver l'effet mauve dans la BottomNavigationView -->
    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemActiveIndicatorStyle">@style/NoActiveIndicator</item>
    </style>

    <!-- Désactive l’indicateur mauve de sélection -->
    <style name="NoActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="TextStyle">
        <item name="android:textColor">#000000</item> <!-- Texte noir -->
        <item name="android:textSize">16sp</item>
    </style>

</resources>
